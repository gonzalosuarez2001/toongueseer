{
  "name": "cron",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "dev": "NODE_ENV=development nodemon app.ts",
    "build": "npx tsc",
    "prd": "NODE_ENV=production pm2 start dist/app.js --name cron-toonguesser",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma-update": "npx prisma db pull && npx prisma generate"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "node-cron": "^4.2.1",
    "nodemon": "^3.1.10",
    "prisma": "^6.16.2",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/node": "^24.5.1",
    "install": "^0.13.0",
    "npm": "^11.6.0"
  }
}